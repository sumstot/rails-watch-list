<div class="container">
  <%= simple_form_for [@list, @bookmark] do |f| %>
    <%= f.association :movie, include_blank: false, collection: @movies  # add collections to alphabatize   %>
    <%= f.input :comment %>
    <%= f.submit  %>
  <% end %>
</div>
